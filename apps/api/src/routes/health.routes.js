const { Router } = require("express");
const router = Router();

// ⚠️ Stub para satisfazer a estrutura. Seu /internal/health já funciona.
// Se quiser usar este arquivo, descomente abaixo e importe no index.js.
// router.get("/internal/health", (req,res)=> res.json({ status:"ok", timestamp: new Date().toISOString() }));

module.exports = router;
