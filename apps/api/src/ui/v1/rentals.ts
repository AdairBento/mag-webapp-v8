import { Router } from "express";

// Stub temporário só para destravar o build.
// Troque por implementação real depois.
export const rentalsRouter = Router();

// Exemplo opcional de health local do módulo (não obrigatório)
// rentalsRouter.get("/_stub", (req, res) => res.json({ ok: true }));

export default rentalsRouter;
